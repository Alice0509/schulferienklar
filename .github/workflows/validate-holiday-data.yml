name: Validate holiday data

on:
  pull_request:
    paths:
      - "data/holidays/**"
      - "data/public-holidays/**"
      - "scripts/validate-holidays.mjs"
      - "scripts/validate-public-holidays.mjs"
      - ".github/workflows/validate-holiday-data.yml"
  push:
    branches:
      - main
    paths:
      - "data/holidays/**"
      - "data/public-holidays/**"
      - "scripts/validate-holidays.mjs"
      - "scripts/validate-public-holidays.mjs"
      - ".github/workflows/validate-holiday-data.yml"

jobs:
  validate-holiday-data:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Validate school holiday data
        run: node scripts/validate-holidays.mjs

      - name: Validate public holiday data
        run: node scripts/validate-public-holidays.mjs
