name: Data Check

on:
  pull_request:
    paths:
      - 'data/**'
      - 'docs/data-policy.md'

jobs:
  data-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder data check
        run: |
          echo "Data validation scripts will be added later."
          test -f docs/data-policy.md
